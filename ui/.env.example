# Copy this file to ui/.env and fill in the values.
# Vite exposes VITE_* variables to the browser bundle at build time.

# Must match API_SECRET_KEY in the root .env.
# Leave blank to disable auth (fine for local dev).
VITE_API_KEY=
